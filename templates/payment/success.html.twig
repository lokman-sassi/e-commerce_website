{% extends 'base.html.twig' %}


{% block body %}
    <div class="container my-5 text-center">
        <img src="/assets/img/check.png" alt="Commande confirmée">
        <h1>Merci pour votre commande</h1>
        <a href="{{ path('app_account_order', { 'id_order' : order.id}) }}" class="btn btn-success mt-4">Consulter ma commande</a>
    </div>
{% endblock %}
